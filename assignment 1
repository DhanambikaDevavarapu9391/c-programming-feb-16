//Program to find factors of a number
#include<stdio.h>
#include<conio.h>
main()
{
	int num,count=1;
	printf("Enter a positive number :\n");
	scanf("%d",&num);
	printf("Factors of %d are:",num);
	while(count<=num)
	{
		if(num%count==0)
		{
			printf("%d\n",count);
		}
		count++;
	}
}
